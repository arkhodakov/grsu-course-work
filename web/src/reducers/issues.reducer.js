const initialState = {
    issues: [],
    projectsIssues: []
}

export default function issues(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}